# Qassas — Web Application

Next.js frontend for the Qassas anomaly detection system.

See the [root README](../README.md) for full setup and run instructions.

## Quick start

```bash
cp .env.example .env   # fill in your values
npm install
npx prisma generate
npx prisma db push
npm run dev
```

App runs at http://localhost:3000
